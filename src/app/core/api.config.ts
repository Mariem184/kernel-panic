/**
 * Base URL of the KernelPanic .NET API.
 * Change it here once (e.g. to http://localhost:5000/api for local backend development).
 */
export const API_BASE = 'https://kernelpanic-portfolio.runasp.net/api';
